import { supabase } from './src/lib/supabase'; async function run() { const { data, error } = await supabase.from('business_rules').select('*'); console.log('Data:', data); console.log('Error:', error); } run();